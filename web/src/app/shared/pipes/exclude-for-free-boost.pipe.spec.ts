import { ExcludeForFreeBoostPipe } from './exclude-for-free-boost.pipe';

describe('ExcludeForFreeBostPipe', () => {
  it('create an instance', () => {
    const pipe = new ExcludeForFreeBoostPipe();
    expect(pipe).toBeTruthy();
  });
});
