import { ToFormGroupPipe } from './to-form-group.pipe';

describe('ToFormGroupPipe', () => {
  it('create an instance', () => {
    const pipe = new ToFormGroupPipe();
    expect(pipe).toBeTruthy();
  });
});
