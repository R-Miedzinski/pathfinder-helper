import { ModifierPlusMinusPipe } from './modifier-plus-minus.pipe';

describe('ModifierPlusMinusPipe', () => {
  it('create an instance', () => {
    const pipe = new ModifierPlusMinusPipe();
    expect(pipe).toBeTruthy();
  });
});
