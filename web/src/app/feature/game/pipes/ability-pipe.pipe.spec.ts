import { AbilityPipePipe } from './ability-pipe.pipe';

describe('AbilityPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AbilityPipePipe();
    expect(pipe).toBeTruthy();
  });
});
